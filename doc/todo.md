# TODO

## Активные

- **#2** Проверка расчёта скорости (корректность физики)
- **#23** Стоянки от дождя: считать два варианта одновременно — с дождевой стоянкой и без. Радиокнопка переключает отображение между вариантами (без перезапуска расчёта). Кнопка удаления стоянки меняет «канонический» вариант и запускает пересчёт.
- **#4** Скорректировать расчёт мощности — модель усталости (мощность постоянна, не учитывает накопленную усталость)
- **#15** Локальный тестовый режим: прогон симуляции маршрута и расчёта мощности на тестовых данных без деплоя на VPS (частично разблокирован — `app.py` импортируется на 3.8 после фикса zoneinfo)
- **#27** Доработать расписание/логирование под измерение латентности моделей (детекции садятся на края quiet-зон — см. [decisions.md](decisions.md) «Анализ weather_log»; данные теперь копит и архив #26)
- **#31** Проверить трек по мощности реального гонщика — прогнать симуляцию/калибровку на реальных данных мощности (данные предоставит пользователь), сверить с физикой (#2/#4)

## Сделано

- ~~**#1**~~ Отображение имени GPX-файла в заголовке страницы (вместо «Целина 600»)
- ~~**#3**~~ Разделение fetch_weather / simulate_only — API только по расписанию, всё остальное пересчитывает без запросов
- ~~**#5**~~ Счётчик API-запросов «N / 10 000» в шапке
- ~~**#6**~~ Ручная установка мощности через форму + лог power_log.jsonl
- ~~**#7**~~ ~~Плановая ночёвка~~ — удалена из расчётов и UI (больше не нужна)
- ~~**#9**~~ ~~best_match модель~~ — удалена (API не возвращает имя конкретной модели)
- ~~**#10**~~ Время старта задавать вручную; ориентировочное время финиша вместо лимита в часах (КВ)
- ~~**#11**~~ AJAX-поллинг меток «симуляция» и «погода» — обновляются без перезагрузки страницы после нажатия кнопок
- ~~**#12**~~ Загрузка произвольного GPX-файла через форму
- ~~**#13**~~ ~~Остаток лимита точек через API~~ — недоступно в бесплатном плане
- ~~**#14**~~ ~~Архив погоды для местности~~ — недоступно в бесплатном плане
- ~~**#16**~~ Две модели погоды параллельно: DWD/ICON и ECMWF IFS, вкладки в интерфейсе
- ~~**#17**~~ Плановые стоянки: суммарный бюджет времени, равномерно раз в час езды; фактические вычитаются из оставшегося
- ~~**#18**~~ Длительность стоянки в часах + минутах отдельными полями
- ~~**#19**~~ Калибровка мощности от предыдущей позиции (а не от нуля)
- ~~**#20**~~ Местное время (по координатам текущей точки маршрута) для всех меток времени в шапке
- ~~**#21**~~ Аудит точек входа к API: update_position больше не делает фетч, использует кеш; добавлен _fetch_lock против параллельных запросов
- ~~**#8**~~ Нелинейная сетка точек погоды: ближайшие 100 км каждые 4 км, дальше плавно до 25 км
- ~~**#22**~~ Направление ветра на 10км-отрезке — корректно: bearing покоординатно для каждого микросегмента GPX
- ~~**#24**~~ Финиш: два варианта по каждой модели — «с дождём» и «без дождя», подпись над цифрой
- ~~**#25**~~ Дешёвая проба обновления модели: фетч A=min(20, ⌈√N⌉) случайных точек вместо всей сетки; полный фетч только при обнаружении изменения
- ~~**#26**~~ Архив статистики заезда (`archive/<ride_id>/`): forecast.jsonl (снимок прогноза), position.jsonl (факт. скорость + live_pred + lead), power_log с входами калибровки, meta.json (реплей). Реализовано, задеплоено, пишет с 19.06
- ~~**#28**~~ Деплой #25+#26 на VPS (`velo_weather`) 19.06.2026 — проверено: новый код работает, архив создаётся для заезда 2026-06-20
- ~~**#29**~~ Зомби-сервис `velo.service` погашен (`disable --now`); активен только `velo_weather`
- ~~**#30**~~ Стоп-гап: снимок ТЕКУЩЕЙ погоды каждые 30 мин в `weather_now.jsonl` (из кеша прогноза, нулевой расход API) — копит ряд погоды-«истины» для местности. Реализовано + задеплоено 19.06
